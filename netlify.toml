# Netlify configuration for Next.js (OpenNext adapter)
# https://docs.netlify.com/build/frameworks/framework-setup-guides/nextjs/

[build]
  command = "npm install && npm run build"
  publish = ".next/server/app"

[build.environment]
  # NEWSLETTER_PROVIDER holds a provider name (e.g. "convertkit"), not a credential.
  # It's legitimately used in server output, so exclude it from secrets scanning.
  SECRETS_SCAN_OMIT_KEYS = "NEWSLETTER_PROVIDER,NEXT_PUBLIC_GSC_VERIFICATION,NEXT_PUBLIC_APP_URL"

[functions]
  node_bundler = "esbuild"
  external_node_modules = ["sharp", "pg", "pg-native"]

[[headers]]
  for = "/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-Content-Type-Options = "nosniff"
    Referrer-Policy = "strict-origin-when-cross-origin"
    Permissions-Policy = "camera=(), microphone=(), geolocation=(), interest-cohort=()"
    X-DNS-Prefetch-Control = "on"
    Strict-Transport-Security = "max-age=63072000; includeSubDomains; preload"
    X-XSS-Protection = "1; mode=block"

[[headers]]
  for = "/_next/static/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/public/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

[[headers]]
  for = "/images/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"

# Removed problematic redirects that cause ERR_TOO_MANY_REDIRECTS
# Next.js handles trailing slashes via next.config.ts

# Enable skew protection (requires Netlify Pro+)
# [build.environment]
#   NETLIFY_NEXT_SKEW_PROTECTION = "true"